>Untuk menjalankan file
robot -d Results /path/path/file.robot

>Menjalankan spesifik test
robot -d Results --include tagname /path/path/file.robot

>Menjalankan spesifik testcase dari test suit
robot -d Results -t "tescase name" /path/path/file.robot